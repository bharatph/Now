package com.thing.now

object Test {
    fun getUserFromCollection(){
        /*
        getUser("uid").when { user ->
            getFriends(user.connections).when { users ->
                friends.
            }
        }
        */
        //fetch userRef.collection("...")....addOnSuccessListener {
        //load user: User = it.toObject(User::java.class)
        //display userName.text = user.name
        //upload user.name = userName.text; uploadUser(user, "uid")
    }
}